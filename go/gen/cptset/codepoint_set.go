// Generated by: main
// TypeWriter: set
// Directive: +gen on Codepoint

package cptset

// Set is a modification of https://github.com/deckarep/golang-set
// The MIT License (MIT)
// Copyright (c) 2013 Ralph Caraveo (deckarep@gmail.com)

// CodepointSet is the primary type that represents a set
type CodepointSet map[Codepoint]struct{}

// NewCodepointSet creates and returns a reference to an empty set.
func NewCodepointSet(a ...Codepoint) CodepointSet {
	s := make(CodepointSet)
	for _, i := range a {
		s.Add(i)
	}
	return s
}

// ToSlice returns the elements of the current set as a slice
func (set CodepointSet) ToSlice() []Codepoint {
	var s []Codepoint
	for v := range set {
		s = append(s, v)
	}
	return s
}

// Add adds an item to the current set if it doesn't already exist in the set.
func (set CodepointSet) Add(i Codepoint) bool {
	_, found := set[i]
	set[i] = struct{}{}
	return !found //False if it existed already
}

// Contains determines if a given item is already in the set.
func (set CodepointSet) Contains(i Codepoint) bool {
	_, found := set[i]
	return found
}

// ContainsAll determines if the given items are all in the set
func (set CodepointSet) ContainsAll(i ...Codepoint) bool {
	for _, v := range i {
		if !set.Contains(v) {
			return false
		}
	}
	return true
}

// IsSubset determines if every item in the other set is in this set.
func (set CodepointSet) IsSubset(other CodepointSet) bool {
	for elem := range set {
		if !other.Contains(elem) {
			return false
		}
	}
	return true
}

// IsSuperset determines if every item of this set is in the other set.
func (set CodepointSet) IsSuperset(other CodepointSet) bool {
	return other.IsSubset(set)
}

// Union returns a new set with all items in both sets.
func (set CodepointSet) Union(other CodepointSet) CodepointSet {
	unionedSet := NewCodepointSet()

	for elem := range set {
		unionedSet.Add(elem)
	}
	for elem := range other {
		unionedSet.Add(elem)
	}
	return unionedSet
}

// Intersect returns a new set with items that exist only in both sets.
func (set CodepointSet) Intersect(other CodepointSet) CodepointSet {
	intersection := NewCodepointSet()
	// loop over smaller set
	if set.Cardinality() < other.Cardinality() {
		for elem := range set {
			if other.Contains(elem) {
				intersection.Add(elem)
			}
		}
	} else {
		for elem := range other {
			if set.Contains(elem) {
				intersection.Add(elem)
			}
		}
	}
	return intersection
}

// Difference returns a new set with items in the current set but not in the other set
func (set CodepointSet) Difference(other CodepointSet) CodepointSet {
	differencedSet := NewCodepointSet()
	for elem := range set {
		if !other.Contains(elem) {
			differencedSet.Add(elem)
		}
	}
	return differencedSet
}

// SymmetricDifference returns a new set with items in the current set or the other set but not in both.
func (set CodepointSet) SymmetricDifference(other CodepointSet) CodepointSet {
	aDiff := set.Difference(other)
	bDiff := other.Difference(set)
	return aDiff.Union(bDiff)
}

// Clear clears the entire set to be the empty set.
func (set *CodepointSet) Clear() {
	*set = make(CodepointSet)
}

// Remove allows the removal of a single item in the set.
func (set CodepointSet) Remove(i Codepoint) {
	delete(set, i)
}

// Cardinality returns how many items are currently in the set.
func (set CodepointSet) Cardinality() int {
	return len(set)
}

// Iter returns a channel of type Codepoint that you can range over.
func (set CodepointSet) Iter() <-chan Codepoint {
	ch := make(chan Codepoint)
	go func() {
		for elem := range set {
			ch <- elem
		}
		close(ch)
	}()

	return ch
}

// Equal determines if two sets are equal to each other.
// If they both are the same size and have the same items they are considered equal.
// Order of items is not relevent for sets to be equal.
func (set CodepointSet) Equal(other CodepointSet) bool {
	if set.Cardinality() != other.Cardinality() {
		return false
	}
	for elem := range set {
		if !other.Contains(elem) {
			return false
		}
	}
	return true
}

// Clone returns a clone of the set.
// Does NOT clone the underlying elements.
func (set CodepointSet) Clone() CodepointSet {
	clonedSet := NewCodepointSet()
	for elem := range set {
		clonedSet.Add(elem)
	}
	return clonedSet
}
